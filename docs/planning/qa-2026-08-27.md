# 2026-08-27 PR #123 실기기 / 에뮬레이터 QA 체크리스트

- **대상 PR**: [PR #123 (feat/full-api-integration)](https://github.com/Triplog11/triplog-app/pull/123)
- **테스트 환경**: Android Emulator (AVD `triplog`, 1080x2400, ko-KR), Expo 54 Dev Client
- **백엔드 API**: `https://triplog11.store` (Production Server)

---

## QA 시나리오 검증 체크리스트

| 번호 | 시나리오 | 상태 | 상세 내용 및 결과 |
| :---: | :--- | :---: | :--- |
| **1** | **이메일 가입 → 자동 로그인 → 홈 진입** | `PASS` | 이메일/비밀번호/닉네임 중복확인 후 주소 바텀시트(`RegionPicker`) 선택 정상 동작. 가입 완료 즉시 자체 로그인 수행되어 온보딩 약관 후 홈 화면으로 정상 진입. |
| **2** | **홈 미션 스트립 진행도 표시** | `PASS` | `GET /missions/me` 실연동. 진행 중인 오늘의 미션 카드 및 진행도 바(XP/스코어 보상) 정상 렌더링. |
| **3** | **도감 지역 탭 → 시군구 → 랜드마크 카드 상세** | `PASS` | 전국 지도 및 시·도 목록 조회 정상 작동. 랜드마크 선택 시 등급별 카드 프레임(`frame-{tier}.png`) 및 `PhotoPlaceholder` 기본 이미지 정상 표시. |
| **4** | **인증: 지역 → 랜드마크 → 위치 → 기록 → 완료 보상** | `PASS` | 5단계 인증 플로우(지역 선택 → 랜드마크 선택 → 위치 확인 → 기록 작성 → 완료 보상 화면) 정상 진행. 멱등성 키 기반 중복 방지 동작. |
| **5** | **완료 후 도감/마이 수치 갱신** | `PASS` | 인증 완료 후 메인 홈 복귀 시 전국 방문율/경험치/수집 카드 수가 실시간 갱신됨. |
| **6** | **랭킹 전체/월간 전환 + 당겨서 새로고침** | `PASS` | 전체 랭킹 및 월간 랭킹 탭 전환 정상. `RefreshControl` 당겨서 새로고침 시 부드러운 인디케이터 동작. |
| **7** | **마이: 칭호 선택 / 대표 뱃지 설정 / 이벤트 / 활동 내역** | `PASS` | 칭호 탭 시 대표 칭호 즉시 반영, 롱프레스 대표 뱃지 설정, 이벤트 목록/상세 플레이스홀더 배너, 활동 내역 무한 스크롤 정상 동작. |
| **8** | **알림 설정 토글 왕복** | `PASS` | 알림 설정 화면 진입 시 푸시/마케팅 알림 Switch 토글 정상 동작 및 백엔드 설정 반영. |
| **9** | **로그아웃 → 재로그인** | `PASS` | 프로필 화면 하단 로그아웃 탭 시 세션 정리 및 로그인 화면 전환 정상. 재로그인 시 토큰 복원 정상 작동. |

---

## 에셋 및 UI 개선 검증 항목
- [x] **A-1**: 랜드마크 카드 기본 이미지(`default-landmark-card.png`) fallback 연동
- [x] **A-2**: 등급별 카드 프레임 4종(`Common`, `Rare`, `Epic`, `Legendary`) 오버레이 연동
- [x] **A-3**: 뱃지 6종 및 칭호 4종 로컬 2톤 플랫 아이콘 및 fallback 헬퍼 연동
- [x] **A-4**: 빈 상태 일러스트 3종(`collection`, `travel-log`, `event`) 연동
- [x] **A-5**: 이벤트 배너 플레이스홀더 2종(`banner-placeholder-{1,2}.png`) 교차 연동
- [x] **A-6**: `frontend/app.json` 스플래시 및 아이콘 경로 점검, `_unused/` 이동, `.gitignore` 갱신
- [x] **A-7**: `docs/design/store/` 1080x1920 스토어 스크린샷 HTML+CSS 템플릿 5장 제작
- [x] **B-1**: 금지어 전수 교체 및 계정/인증 화면 `~합니다` 체, 탐험/도감 `~해요` 체 통일
- [x] **B-2**: `RegionPicker` 바텀시트 검색 공통 컴포넌트 적용
- [x] **B-3**: `README.md` 프론트 로컬 실행 가이드 작성
- [x] **B-4**: `jest-expo` 설정 및 5개 유틸 단위 테스트 28개 전체 통과 (`npm test`)
- [x] **B-5**: 28개 전체 화면 1080x2400 PNG 스크린샷 캡처 완료
- [x] **C-1**: Python 표준 라이브러리 기반 `scripts/smoke-api.py` 작성
